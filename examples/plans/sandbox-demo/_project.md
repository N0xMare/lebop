---
name: "TEST plan: sandbox demo"
description: "Ephemeral plan used to verify `lebop plan apply`. Safe to archive."
state: backlog
team: UE
---

# Sandbox demo project

This project is generated by `lebop plan apply` for regression testing of the
declarative-graph feature. It pairs with `UE-359` and `UE-360` (the persistent
sentinels) as external link targets.
